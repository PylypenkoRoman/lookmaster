import { EmailTDValidator } from './emailTD.validator';

describe('MailTempDrivenDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailTDValidator();
    expect(directive).toBeTruthy();
  });
});
